[package]
name = "zeroterm-plugin"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Sandboxed WASM plugin runtime (wasmtime + WASI) for ZeroTerm"

[dependencies]
anyhow = { workspace = true }
wasmtime = { workspace = true }
wasmtime-wasi = { workspace = true }

[dev-dependencies]
wat = { workspace = true }
