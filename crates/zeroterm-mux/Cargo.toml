[package]
name = "zeroterm-mux"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Tabs, splits, session management for ZeroTerm"

[dependencies]
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }