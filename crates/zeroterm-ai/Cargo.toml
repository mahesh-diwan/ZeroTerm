[package]
name = "zeroterm-ai"
version = "0.3.4"
edition = "2021"
license = "MIT"
description = "Local AI integration (Ollama/LM Studio) for ZeroTerm"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
reqwest = { version = "0.12", features = ["json", "rustls-tls", "blocking"] }

[dev-dependencies]
criterion = { workspace = true }